<h2>All Photos</h2>

<div id="container" class="m-auto w-8 p1">
  <% @photos.each do |photo| %>
    <div class="box">
      <div class="thumb">
        <%= link_to image_tag(photo.photo.url(:medium)), photo_path(photo.id) %></a>
        <p><%= link_to photo.title, photo_path(photo.id) %></p>
      </div>
    </div>
  <% end %>
</div>
